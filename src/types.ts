export type Artwork = {
    objectID : number;
    title: string;
    medium: string;
    primaryImage: string;
    isHighlight: boolean;
    objectURL: string;
};

export type IDs = {
    objectIDs: number[];
};

